package main

import "fmt"

// This file was replaced with a small stub to avoid import/build issues
// The migration CLI has been moved to `cmd/migrate`.
func main() {
	fmt.Println("Migration helper moved to ./cmd/migrate; run: go run ./cmd/migrate <command>")
}
